# Client CRUD React (Vite) - Mock Version

Proyecto de ejemplo con **React 19**, **Vite** y **TailwindCSS** que simula un backend local con `src/assets/clients.json`.

## Ejecutar

1. `npm install`
2. `npm run dev`
3. Abrir `http://localhost:5173`

> El proyecto está preparado para usar un backend real cambiando las llamadas de mock por `fetch('/api/clients')`.

